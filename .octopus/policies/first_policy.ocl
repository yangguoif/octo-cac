name = "First policy"
violation_action = "block"

conditions {
    rego = <<-EOT
            package first_policy
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package first_policy
            
            default evaluate := true
            EOT
}