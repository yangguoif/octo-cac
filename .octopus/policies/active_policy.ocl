name = "Active policy"
violation_action = "block"

conditions {
    rego = <<-EOT
            package active_policy
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package active_policy
            
            default evaluate := true
            EOT
}