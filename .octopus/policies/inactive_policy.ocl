name = "Inactive policy"
violation_action = "block"

conditions {
    rego = <<-EOT
            package inactive_policy
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package inactive_policy
            
            default evaluate := true
            EOT
}