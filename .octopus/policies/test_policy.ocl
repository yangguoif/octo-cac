name = "Test policy"
violation_action = "block"

conditions {
    rego = <<-EOT
            package test_policy
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package test_policy
            
            default evaluate := true
            EOT
}