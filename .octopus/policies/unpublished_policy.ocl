name = "Unpublished policy"
violation_action = "block"

conditions {
    rego = <<-EOT
            package unpublished_policy
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package unpublished_policy
            
            default evaluate := true
            EOT
}